import type { VideoData } from "../scenes/types";
import demo from "./demo";

/** Demo videosunun dikey (9:16) varyanti. */
const video: VideoData = { ...demo, format: "reels" };

export default video;
